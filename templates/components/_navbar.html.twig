<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerMenu" aria-controls="navbarTogglerMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerMenu">
            <ul class="navbar-nav">
                <li class="nav-item">
                <a class="nav-link" href="{{ path('project_index') }}">Projets</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('profile') }}">Mon profil</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('contact_new') }}">Formulaire de contact</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('project_new') }}">Ajouter un projet</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('login') }}">Se connecter</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>